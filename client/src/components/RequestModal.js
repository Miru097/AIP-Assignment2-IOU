import React, { Component } from 'react';
import { connect } from 'react-redux';
class RequestModal extends Component {
    render() {
        return (
            <p>aa</p>
        )
    }
}
export default connect(null)(RequestModal);